import styled from "styled-components";
import tw from "twin.macro";

export const OTPInputContainer = styled.div`
  ${tw``}
`;

export const Error = styled.p`
  ${tw`mt-1 text-lg font-medium text-sematic-1`}
`;
