import styled from "styled-components";
import tw from "twin.macro";

export const NotFoundContainer = styled.div`
  ${tw`  `}
`;

export const Title = styled.div`
  ${tw`font-bold text-neutral-1 text-lg text-center`}
`;
