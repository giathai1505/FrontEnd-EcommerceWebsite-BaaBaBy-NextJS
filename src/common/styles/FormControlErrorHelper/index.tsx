import styled from "styled-components";
import tw from "twin.macro";

const FormControlErrorHelper = styled.p`
  ${tw` text-sematic-1 text-xs ml-1.5 mt-0.5`}
`;

export default FormControlErrorHelper;
