import styled from "styled-components";
import tw from "twin.macro";

export const MenuItemContainer = styled.a`
  ${tw`relative flex items-center gap-[8px] py-2 text-md font-semibold leading-tight cursor-pointer`}
`;
